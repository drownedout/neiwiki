<div class="row home_title_row">
	<div class="col-md-4 home_row">
		<h4 class="home_title"><%= current_user.firstname %>'s Article Comments (<%= current_user.comments.count %>)</h4>
	</div>
</div>
<% if current_user.comments.any? %>
	<% current_user.comments.each do |comment| %>
		<div class="comment">
			<h4><%= comment.article.title %></h4>
			<p class="comment_time"><%=comment.created_at.strftime('%b %d, %Y') %></p>
			<p><%= comment.body %></p>
		</div>
	<% end %>
<% else %>
	<p>You have not written any comments yet.</p>
<% end %>