%span= comment.user.firstname
%span= comment.user.lastname
%br
%span= time_ago_in_words(comment.created_at)
%span Ago
%br
%br
%p= comment.body
- if current_user.id == comment.user_id
	%p= link_to 'Delete', [comment.article, comment], method: :delete, class: "btn btn-danger", data: { confirm: "Are you sure?"}